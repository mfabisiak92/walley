package com.walley.app.core.ui

import androidx.compose.ui.graphics.Color

/**
 * Investment gain/loss tint, shared by the Home screen's Investments card and the Accounts screen's
 * Investments tab summary. [InvestmentNeutralColor] is used when the gain/loss is exactly zero.
 */
val InvestmentGainColor = Color(0xFF2E7D32)
val InvestmentNeutralColor = Color(0xFF1565C0)
